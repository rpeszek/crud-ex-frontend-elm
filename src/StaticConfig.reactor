{-
  Version of StaticConfig used when running inside elm-reactor. 
  (Elm is served from a separate web server.)
  Defines base server URL.
-}
module StaticConfig exposing (..)

serverBase = "http://localhost:3000"  
